---
name: Formatter for Terraform
on:
  workflow_call:
    inputs:
      search-path:
        required: false
        type: string
        description: Path to search for files
        default: .
      terraform-version:
        required: false
        type: string
        description: Terraform version to use
        default: latest
      lint-before-pr:
        required: false
        type: boolean
        description: Run lint before creating a pull request
        default: true
defaults:
  run:
    shell: bash -euo pipefail {0}
    working-directory: .
jobs:
  format-and-pr:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v3
        with:
          terraform-version: ${{ inputs.terraform-version }}
      - name: Install tflint
        if: ${{ inputs.lint-before-pr }}
        run: |
          curl -sSL https://raw.githubusercontent.com/terraform-linters/tflint/master/install_linux.sh | bash
      - name: Format the codes using terraform fmt
        run: |
          terraform fmt -recursive ${{ inputs.search-path }}
      - name: Lint the codes using tflint
        if: ${{ inputs.lint-before-pr }}
        run: |
          tflint --recursive --filter ${{ inputs.search-path }}
      - name: Create a commit message
        id: commit-message
        run: |
          echo "COMMIT_MESSAGE=Reformat Terraform codes using terraform fmt" >> "${GITHUB_OUTPUT}"
      - name: Detect changed files and create a pull request
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: ${{ steps.commit-message.outputs.COMMIT_MESSAGE }}
          branch: feature/terraform-format-${{ github.run_id }}
          title: ${{ steps.commit-message.outputs.COMMIT_MESSAGE }}
          body: |
            This PR contains the following updates:

            - ${{ steps.commit-message.outputs.COMMIT_MESSAGE }}

            This PR has been generated by
            [${{ github.workflow_ref }}](https://github.com/${{ github.repository }}/).
          delete-branch: true
          draft: false
